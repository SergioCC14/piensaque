<%- if !@users.blank? %>
  $(".search_list").html("<%= escape_javascript render(:partial => 'users/search', :locals => {:users => @users}) %>");

<%- else %>
  $('.search_list').html("<tr><td>No se han encontrado usuarios</tr>")

<%- end %>